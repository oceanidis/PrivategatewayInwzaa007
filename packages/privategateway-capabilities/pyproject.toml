[build-system]
requires = ['setuptools>=69']
build-backend = 'setuptools.build_meta'

[project]
name = 'privategateway-capabilities'
version = '0.2.0'
requires-python = '>=3.11'
dependencies = [
  'privategateway-client>=0.2,<0.3',
  'privategateway-protocol>=0.2,<0.3',
]

[tool.setuptools]
packages = []
